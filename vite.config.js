import { defineConfig } from 'vite';

export default defineConfig({
  // If deploying to https://USERNAME.github.io/REPO-NAME set base to '/REPO-NAME/'
  // If deploying to https://USERNAME.github.io (user page) keep as '/'
  base: '/JB-Website/',
});
